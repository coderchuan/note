## 迭代器
* 用途:在容器中使用,用于遍历容器内的元素
* 获取迭代器
    * 使用容器实例(假设为`obj`)调用方法`obj.__iter__()`
    * 
* 迭代器需要支持的方法(设迭代器的实例为ite)
    * `ite.__iter__()`:返回迭代器本身
    * `iterator.__next__()`:返回迭代器的下一项。当访问完最后一个元素时,此方法将会抛出`StopIteration`异常以终止循环