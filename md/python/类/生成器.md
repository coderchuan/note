## 生成器
* 用途:生成与原容器无关联的实例
* 获取生成器(设容器实例为obj) 
    * `(i*i for i in obj)`:生成器实例元素值为obj中每个元素的平方 
    * `((x,y) for x in range(1,5) for y in range(1,10) if x!=y)`:获取x范围为1-4,y的范围为1-9,且x不等于y的元组生成器实例 
* 使用生成器:见迭代器 