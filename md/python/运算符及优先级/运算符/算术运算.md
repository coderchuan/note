## 算术运算
* 单目运算
    * `+`:正数,在未格式化的情况下,将不会有`+`号输出
    * `-`:负数,将不会有`-`号输出
* 双目运算
    * `+`:加法
    * `-`:减法
    * `*`:乘法。两个参数必须为数字或一个为数字另一个为序列 
    * `/`:除法
    * `//`:整除。将`/`的结果使用`floor()`函数进行处理。分母为零将抛出`ZeroDivisionError`
    * `@`:矩阵乘法
    * `%`:取余
    * `**`:幂运算(指数运算)。如`5 ** 2`的结果为`25`