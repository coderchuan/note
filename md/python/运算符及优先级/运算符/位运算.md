## 位运算
* 一元运算
    * `~`:按位取反
* 二元运算
    * `&`:与,左右两个位都为1则为1,否则为0
    * `|`:或,左右两个位有一个为1则为1,否则为0 
    * `^`:异或,左右两个相同则为1,否则为0
    * `>>`:右移,相当于乘以`pow(2,-n)`(n表示右移的第二个参数) 
    * `<<`:右移,相当于乘以`pow(2,n)`(n表示右移的第二个参数) 