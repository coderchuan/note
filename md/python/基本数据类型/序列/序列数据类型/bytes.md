## 字节序列
* 关键字:bytes
* 类型:不可变序列 
* 名称:字节序列 
* 注意:bytes中的每个字节都表示一个8位二进制,因此字面值只能为ascii字符,若值大于127则应使用转义表示
* 字面值
    * 双引号 
        * 示例:b"abc,'def'"
    * 单引号
        * 示例:b'abc,"def"'
    * 三引号
        * 示例
            * b'''abc,"def1","ghi2"'''
            * b"""abc,'def1','ghi2'"""