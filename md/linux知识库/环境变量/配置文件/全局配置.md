## 全局配置文件
* `/etc/environment`
    * 文件类型:纯文本,只能写为`KEY_NAME="VALUE"`形式,如`PATH="/usr/bin"`。可用`#`注释
    * 加载时机:用户登录时 
* `/etc/profile`:当有用户登录时,操作系统加载的第二个环境配置文件
    * `/etc/bash.bashrc`:此文件被`/etc/profile`文件调用加载 
