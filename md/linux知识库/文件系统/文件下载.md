# 命令行下载普通文件 
* 命令
    * 保留原文件名
        * curl -O URL  
        * wget URL 
    * 重新命名
        * curl -o NEW_FILE_NAME URL 
        * wget -O NEW_FILE_NAME URL 
* 解释
    * `URL`:文件URL地址
    * `NEW_FILE_NAME`:新的文件(路径)名 